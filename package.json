{
  "name": "js-meme",
  "version": "1.0.0",
  "description": "Tiny meme renderer (client + server CLI)",
  "main": "server-render.js",
  "scripts": {
    "render": "node server-render.js",
    "start": "node server.js",
    "check-native": "node check-native.js",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "canvas": "^2.9.0",
    "express": "^4.18.2",
    "multer": "^1.4.5"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  }
}
